{
  "name": "crimeminister-take-home-exercise",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "db:migrations:generate": "drizzle-kit generate",
    "db:migrations:migrate": "wrangler d1 migrations apply ingestion-db --remote=true"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.8.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.92.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
